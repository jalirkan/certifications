# Normalize line endings in the repository.
#
# Without this, a Windows checkout can rewrite whole files to CRLF and every
# line shows as changed. Text is LF in the repository and in the working tree;
# the index is the single source of truth for line endings.

* text=auto eol=lf
